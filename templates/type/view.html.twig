{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - {{ type.name }}
{% endblock %}

{% block body %}
    <h1 class="mt-5"> Catégories {{ type.name }}</h1>
    <h3 class="mt-5">Produit</h3>
    <div class="list-group">
        {% for product in type.Producttype %}
            <div>
                {% include 'product/list.html.twig' %}
                <a href="{{ path('product_view', {'id': product.id}) }}" class="list-group-item list-group-item-action">

                </a>
            </div>

        {% endfor %}
    </div>
{% endblock %}